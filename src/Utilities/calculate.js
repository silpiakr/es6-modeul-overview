const add = (frist, second) => {
    return frist + second;
}

const multiply = (first, second) => {
    return first * second;
}

const divideFirstNumBySecondNum = (first, second) => {
    return first + second;
}

export {add, multiply, divideFirstNumBySecondNum}

// const calculator = {add: add} 
// export default calculator;
